const {Triangle, Circle, Square} = require('./shapes')

describe('shape', () =>{
    it('should take the color + shape and render the corresponding render()', () => {
        const triangle = new Triangle
        const color = ''
        expect(triangle).toBe(color)
    })
})